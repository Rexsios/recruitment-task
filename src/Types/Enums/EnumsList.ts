export enum WhichColumn {
  DEFAULT,
  ID,
  NAME,
  CITY,
  TOTALINCOMES,
  AVARAGEINCOMES,
  LASTMONTHINCOMES,
}

export enum ChevronType {
  DEFAULT,
  ASCENDING,
  DESCENDING,
}

export enum MessageType {
  GOOD,
  BAD,
}
